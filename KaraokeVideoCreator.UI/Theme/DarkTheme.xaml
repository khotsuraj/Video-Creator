<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

    <!-- Centralized Clean White / Light Theme Brushes -->
    <SolidColorBrush x:Key="WindowBackgroundBrush" Color="#FFFFFF"/>
    <SolidColorBrush x:Key="PanelBackgroundBrush" Color="#FFFFFF"/>
    <SolidColorBrush x:Key="CardBackgroundBrush" Color="#FFFFFF"/>
    <SolidColorBrush x:Key="CardBorderBrush" Color="#E5E7EB"/>

    <SolidColorBrush x:Key="TextPrimaryBrush" Color="#202124"/>
    <SolidColorBrush x:Key="TextSecondaryBrush" Color="#5F6368"/>
    <SolidColorBrush x:Key="TextDisabledBrush" Color="#9CA3AF"/>

    <SolidColorBrush x:Key="InputBackgroundBrush" Color="#FFFFFF"/>
    <SolidColorBrush x:Key="InputTextBrush" Color="#202124"/>
    <SolidColorBrush x:Key="InputBorderBrush" Color="#D1D5DB"/>
    <SolidColorBrush x:Key="InputSelectionBrush" Color="#E0E7FF"/>

    <SolidColorBrush x:Key="MenuBackgroundBrush" Color="#FFFFFF"/>
    <SolidColorBrush x:Key="MenuTextBrush" Color="#202124"/>
    <SolidColorBrush x:Key="MenuItemHoverBrush" Color="#F3F4F6"/>
    <SolidColorBrush x:Key="MenuBorderBrush" Color="#E5E7EB"/>

    <SolidColorBrush x:Key="ToolbarBackgroundBrush" Color="#FFFFFF"/>
    <SolidColorBrush x:Key="ToolbarButtonHoverBrush" Color="#F3F4F6"/>
    <SolidColorBrush x:Key="ToolbarButtonPressedBrush" Color="#E5E7EB"/>

    <!-- Mandatory Rule: Light Button Background, BLACK Text (#000000) -->
    <SolidColorBrush x:Key="ButtonBackgroundBrush" Color="#F3F4F6"/>
    <SolidColorBrush x:Key="ButtonHoverBackgroundBrush" Color="#E5E7EB"/>
    <SolidColorBrush x:Key="ButtonPressedBackgroundBrush" Color="#D1D5DB"/>
    <SolidColorBrush x:Key="ButtonTextBrush" Color="#000000"/>
    <SolidColorBrush x:Key="ButtonDisabledBackgroundBrush" Color="#F9FAFB"/>
    <SolidColorBrush x:Key="ButtonDisabledTextBrush" Color="#9CA3AF"/>

    <!-- Light Accent Button & Active Tab Brushes (BLACK Text) -->
    <SolidColorBrush x:Key="PrimaryAccentButtonBrush" Color="#DBEAFE"/>
    <SolidColorBrush x:Key="PrimaryAccentButtonHoverBrush" Color="#BFDBFE"/>
    <SolidColorBrush x:Key="PrimaryAccentButtonPressedBrush" Color="#93C5FD"/>

    <SolidColorBrush x:Key="ActiveTabBackgroundBrush" Color="#EFF6FF"/>
    <SolidColorBrush x:Key="ActiveTabBorderBrush" Color="#3B82F6"/>

    <SolidColorBrush x:Key="StatusBarBackgroundBrush" Color="#F9FAFB"/>
    <SolidColorBrush x:Key="StatusBarTextBrush" Color="#5F6368"/>

    <SolidColorBrush x:Key="PlayheadBrush" Color="#EF4444"/>
    <SolidColorBrush x:Key="WaveformBarBrush" Color="#2563EB"/>

    <!-- Video Preview Canvas Area Brushes -->
    <SolidColorBrush x:Key="PreviewBackgroundBrush" Color="#FFFFFF"/>
    <SolidColorBrush x:Key="PreviewSurfaceBrush" Color="#09090B"/>
    <SolidColorBrush x:Key="PreviewBorderBrush" Color="#E5E7EB"/>
    <SolidColorBrush x:Key="PreviewIconBrush" Color="#71717A"/>
    <SolidColorBrush x:Key="PreviewTextBrush" Color="#A1A1AA"/>

    <!-- Default Typography -->
    <FontFamily x:Key="AppFontFamily">Segoe UI, Noto Sans, Arial</FontFamily>

    <!-- Implicit Control Styles -->
    <Style TargetType="Window">
        <Setter Property="Background" Value="{StaticResource WindowBackgroundBrush}"/>
        <Setter Property="Foreground" Value="{StaticResource TextPrimaryBrush}"/>
        <Setter Property="FontFamily" Value="{StaticResource AppFontFamily}"/>
        <Setter Property="FontSize" Value="13"/>
    </Style>

    <Style TargetType="TextBlock">
        <Setter Property="Foreground" Value="{StaticResource TextPrimaryBrush}"/>
        <Setter Property="FontFamily" Value="{StaticResource AppFontFamily}"/>
    </Style>

    <Style TargetType="Label">
        <Setter Property="Foreground" Value="{StaticResource TextPrimaryBrush}"/>
        <Setter Property="FontFamily" Value="{StaticResource AppFontFamily}"/>
    </Style>

    <Style TargetType="TextBox">
        <Setter Property="Background" Value="{StaticResource InputBackgroundBrush}"/>
        <Setter Property="Foreground" Value="{StaticResource InputTextBrush}"/>
        <Setter Property="BorderBrush" Value="{StaticResource InputBorderBrush}"/>
        <Setter Property="SelectionBrush" Value="{StaticResource InputSelectionBrush}"/>
        <Setter Property="FontFamily" Value="{StaticResource AppFontFamily}"/>
        <Setter Property="BorderThickness" Value="1"/>
        <Setter Property="Padding" Value="6,4"/>
        <Setter Property="FontSize" Value="13"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="TextBox">
                    <Border x:Name="border"
                            Background="{TemplateBinding Background}"
                            BorderBrush="{TemplateBinding BorderBrush}"
                            BorderThickness="{TemplateBinding BorderThickness}"
                            CornerRadius="2">
                        <ScrollViewer x:Name="PART_ContentHost" Focusable="False"/>
                    </Border>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsFocused" Value="True">
                            <Setter TargetName="border" Property="BorderBrush" Value="#3B82F6"/>
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <Style TargetType="Button">
        <Setter Property="Background" Value="{StaticResource ButtonBackgroundBrush}"/>
        <Setter Property="Foreground" Value="{StaticResource ButtonTextBrush}"/>
        <Setter Property="FontFamily" Value="{StaticResource AppFontFamily}"/>
        <Setter Property="FontWeight" Value="SemiBold"/>
        <Setter Property="Padding" Value="12,6"/>
        <Setter Property="BorderBrush" Value="{StaticResource InputBorderBrush}"/>
        <Setter Property="BorderThickness" Value="1"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="Button">
                    <Border x:Name="border" Background="{TemplateBinding Background}"
                            BorderBrush="{TemplateBinding BorderBrush}"
                            BorderThickness="{TemplateBinding BorderThickness}"
                            CornerRadius="3" Padding="{TemplateBinding Padding}">
                        <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                    </Border>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter TargetName="border" Property="Background" Value="{StaticResource ButtonHoverBackgroundBrush}"/>
                        </Trigger>
                        <Trigger Property="IsPressed" Value="True">
                            <Setter TargetName="border" Property="Background" Value="{StaticResource ButtonPressedBackgroundBrush}"/>
                        </Trigger>
                        <Trigger Property="IsEnabled" Value="False">
                            <Setter TargetName="border" Property="Background" Value="{StaticResource ButtonDisabledBackgroundBrush}"/>
                            <Setter Property="Foreground" Value="{StaticResource ButtonDisabledTextBrush}"/>
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <Style x:Key="ToolbarButtonStyle" TargetType="Button">
        <Setter Property="Background" Value="Transparent"/>
        <Setter Property="Foreground" Value="#000000"/>
        <Setter Property="Padding" Value="6"/>
        <Setter Property="Margin" Value="1,0"/>
        <Setter Property="BorderThickness" Value="1"/>
        <Setter Property="BorderBrush" Value="{StaticResource InputBorderBrush}"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="Button">
                    <Border x:Name="border" Background="{TemplateBinding Background}" BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="{TemplateBinding BorderThickness}" CornerRadius="2" Padding="{TemplateBinding Padding}">
                        <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                    </Border>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter TargetName="border" Property="Background" Value="{StaticResource ToolbarButtonHoverBrush}"/>
                        </Trigger>
                        <Trigger Property="IsPressed" Value="True">
                            <Setter TargetName="border" Property="Background" Value="{StaticResource ToolbarButtonPressedBrush}"/>
                        </Trigger>
                        <Trigger Property="IsEnabled" Value="False">
                            <Setter TargetName="border" Property="Background" Value="Transparent"/>
                            <Setter Property="Opacity" Value="0.5"/>
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <Style TargetType="Menu">
        <Setter Property="Background" Value="{StaticResource MenuBackgroundBrush}"/>
        <Setter Property="Foreground" Value="{StaticResource MenuTextBrush}"/>
        <Setter Property="FontFamily" Value="{StaticResource AppFontFamily}"/>
        <Setter Property="FontSize" Value="13"/>
    </Style>

    <Style TargetType="MenuItem">
        <Setter Property="Foreground" Value="{StaticResource MenuTextBrush}"/>
        <Setter Property="Background" Value="Transparent"/>
        <Setter Property="Padding" Value="10,5"/>
        <Style.Triggers>
            <Trigger Property="IsHighlighted" Value="True">
                <Setter Property="Background" Value="{StaticResource MenuItemHoverBrush}"/>
                <Setter Property="Foreground" Value="#000000"/>
            </Trigger>
            <Trigger Property="IsEnabled" Value="False">
                <Setter Property="Foreground" Value="{StaticResource TextDisabledBrush}"/>
            </Trigger>
        </Style.Triggers>
    </Style>

    <Style TargetType="Separator">
        <Setter Property="Background" Value="{StaticResource CardBorderBrush}"/>
        <Setter Property="Margin" Value="0,2"/>
    </Style>

    <Style TargetType="StatusBar">
        <Setter Property="Background" Value="{StaticResource StatusBarBackgroundBrush}"/>
        <Setter Property="Foreground" Value="{StaticResource StatusBarTextBrush}"/>
        <Setter Property="FontFamily" Value="{StaticResource AppFontFamily}"/>
        <Setter Property="FontSize" Value="12"/>
    </Style>

</ResourceDictionary>
