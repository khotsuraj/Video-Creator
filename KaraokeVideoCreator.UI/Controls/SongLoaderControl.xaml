<UserControl x:Class="KaraokeVideoCreator.UI.Controls.SongLoaderControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="400" d:DesignWidth="240">
    <UserControl.Resources>
        <BooleanToVisibilityConverter x:Key="BoolToVis"/>
    </UserControl.Resources>

    <StackPanel>
        <!-- Left Panel Header -->
        <Border Background="{StaticResource ToolbarBackgroundBrush}" BorderBrush="{StaticResource CardBorderBrush}" BorderThickness="0,0,0,1" Padding="10,6" Margin="0,0,0,8">
            <TextBlock Text="PROJECT MEDIA" FontSize="11" FontWeight="Bold" Foreground="{StaticResource TextSecondaryBrush}"/>
        </Border>

        <Border Background="{StaticResource PanelBackgroundBrush}" BorderBrush="{StaticResource CardBorderBrush}" BorderThickness="1" CornerRadius="3" Padding="10" Margin="8,0,8,8">
            <StackPanel>
                <!-- 1. Audio Track Section -->
                <DockPanel Margin="0,0,0,8">
                    <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                        <TextBlock Text="🎵" FontSize="14" Margin="0,0,6,0"/>
                        <TextBlock Text="Audio Track" FontSize="12" FontWeight="Bold" Foreground="{StaticResource TextPrimaryBrush}"/>
                    </StackPanel>
                    <Button Content="Import..." Command="{Binding ImportAudioCommand}" HorizontalAlignment="Right" Padding="6,2" FontSize="11" Style="{StaticResource ToolbarButtonStyle}"/>
                </DockPanel>

                <!-- Audio Track Item -->
                <Border Background="{StaticResource WindowBackgroundBrush}" BorderBrush="{StaticResource CardBorderBrush}" BorderThickness="1" CornerRadius="2" Padding="8,6" Margin="0,0,0,12">
                    <StackPanel>
                        <TextBlock Text="{Binding AudioFileName}" FontSize="12" FontWeight="SemiBold" Foreground="{StaticResource TextPrimaryBrush}" TextTrimming="CharacterEllipsis"/>
                        <DockPanel Margin="0,4,0,0">
                            <TextBlock Text="Duration:" FontSize="10" Foreground="{StaticResource TextDisabledBrush}"/>
                            <TextBlock Text="{Binding AudioDurationFormatted}" FontSize="11" FontWeight="Bold" Foreground="{StaticResource TextSecondaryBrush}" HorizontalAlignment="Right"/>
                        </DockPanel>
                        <!-- Missing Warning -->
                        <Border Background="#FEF2F2" BorderBrush="#FECACA" BorderThickness="1" CornerRadius="2" Padding="4,2" Margin="0,6,0,0"
                                Visibility="{Binding IsAudioMissing, Converter={StaticResource BoolToVis}}">
                            <TextBlock Text="⚠ File Missing" Foreground="#DC2626" FontSize="10" FontWeight="Bold"/>
                        </Border>
                    </StackPanel>
                </Border>

                <!-- 2. Lyrics Document Section -->
                <DockPanel Margin="0,0,0,8">
                    <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                        <TextBlock Text="📝" FontSize="14" Margin="0,0,6,0"/>
                        <TextBlock Text="Lyrics Document" FontSize="12" FontWeight="Bold" Foreground="{StaticResource TextPrimaryBrush}"/>
                    </StackPanel>
                    <TextBlock Text="{Binding LineCount, StringFormat='{}{0} Lines'}" FontSize="11" Foreground="{StaticResource TextSecondaryBrush}" HorizontalAlignment="Right" VerticalAlignment="Center"/>
                </DockPanel>

                <Border Background="{StaticResource WindowBackgroundBrush}" BorderBrush="{StaticResource CardBorderBrush}" BorderThickness="1" CornerRadius="2" Padding="8,6" Margin="0,0,0,12">
                    <DockPanel>
                        <TextBlock Text="Status:" FontSize="11" Foreground="{StaticResource TextDisabledBrush}"/>
                        <TextBlock Text="{Binding LineCount, StringFormat='{}{0} Lines Entered'}" FontSize="11" FontWeight="SemiBold" Foreground="{StaticResource TextPrimaryBrush}" HorizontalAlignment="Right"/>
                    </DockPanel>
                </Border>

                <!-- 3. Background Asset Section -->
                <DockPanel Margin="0,0,0,8">
                    <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                        <TextBlock Text="🖼" FontSize="14" Margin="0,0,6,0"/>
                        <TextBlock Text="Background" FontSize="12" FontWeight="Bold" Foreground="{StaticResource TextPrimaryBrush}"/>
                    </StackPanel>
                    <TextBlock Text="None" FontSize="11" Foreground="{StaticResource TextDisabledBrush}" HorizontalAlignment="Right" VerticalAlignment="Center"/>
                </DockPanel>
            </StackPanel>
        </Border>
    </StackPanel>
</UserControl>
